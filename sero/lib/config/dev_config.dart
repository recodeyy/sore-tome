// Live-data policy: the former `kUseMockData` UI-preview toggle has been removed.
// The app always talks to the real authenticated, tenant-scoped backend.
// Intentionally left without a mock-mode flag so production builds cannot enable
// fake data.
